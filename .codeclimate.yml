engines:
  eslint:
    enabled: true
    channel: 'eslint-9'
    config:
      config: 'eslint.config.mjs'

checks:
  return-statements:
    enabled: false
  method-lines:
    config:
      threshold: 100
  method-complexity:
    config:
      threshold: 20

ratings:
  paths:
    - '**.js'
