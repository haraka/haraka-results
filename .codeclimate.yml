engines:
  eslint:
    enabled: true
    channel: "eslint-8"
    config:
      config: ".eslintrc.yaml"

checks:
  return-statements:
    enabled: false
  method-lines:
    config:
      threshold: 100
  method-complexity:
    config:
      threshold: 20

ratings:
   paths:
   - "**.js"
