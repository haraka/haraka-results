language: node_js
node_js:
    - "12"

services:
    - redis-server

before_script:

script:
    - npm run lint
    - npm test

after_success:
    - npm install istanbul codecov
    - npm run cover
    - npx codecov

sudo: false
